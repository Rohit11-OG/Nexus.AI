flask>=3.0
flask-cors>=4.0
requests>=2.31          # used by Gemini REST fallback
python-dotenv>=1.0      # loads .env file automatically

# Optional — install whichever provider you want to use:
# pip install openai       # for OpenAI GPT-4o-mini fallback
# pip install anthropic    # for Claude Haiku fallback
# (Gemini works with just `requests` — no extra install needed)
